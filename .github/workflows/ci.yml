name: CI

on:
  pull_request:
    branches: [develop]
  push:
    branches: [develop]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use runner Temurin 25
        run: |
          echo "JAVA_HOME=${JAVA_HOME_25_X64}" >> "$GITHUB_ENV"
          echo "${JAVA_HOME_25_X64}/bin" >> "$GITHUB_PATH"

      - name: Show toolchain
        run: |
          java -version
          mvn -version

      - name: Format
        run: bash .github/scripts/ci/format.sh --check

      - name: Test
        run: bash .github/scripts/ci/test.sh

      - name: Smoke (container must respond)
        run: bash .github/scripts/ci/smoke.sh
